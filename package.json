{
  "name": "goja",
  "version": "10.2.2-2",
  "description": "Goja",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:unit": "vitest run tests/unit",
    "test:watch": "vitest watch",
    "test:coverage": "vitest run --coverage",
    "generate-fixtures": "node scripts/generate-fixtures.js",
    "pretest:e2e": "node scripts/generate-fixtures.js",
    "test:e2e": "node -e \"const os=require('node:os'); const path=require('node:path'); const {spawnSync}=require('node:child_process'); const defaultPath=process.platform==='win32'?path.join(os.homedir(),'AppData','Local','ms-playwright'):process.platform==='darwin'?path.join(os.homedir(),'Library','Caches','ms-playwright'):path.join(os.homedir(),'.cache','ms-playwright'); const env={...process.env, PLAYWRIGHT_BROWSERS_PATH: process.env.PLAYWRIGHT_BROWSERS_PATH || defaultPath}; const cli=path.join(process.cwd(),'node_modules','@playwright','test','cli.js'); const r=spawnSync(process.execPath,[cli,'test'],{stdio:'inherit',env}); process.exit(r.status===null?1:r.status);\"",
    "test:e2e:ui": "node -e \"const os=require('node:os'); const path=require('node:path'); const {spawnSync}=require('node:child_process'); const defaultPath=process.platform==='win32'?path.join(os.homedir(),'AppData','Local','ms-playwright'):process.platform==='darwin'?path.join(os.homedir(),'Library','Caches','ms-playwright'):path.join(os.homedir(),'.cache','ms-playwright'); const env={...process.env, PLAYWRIGHT_BROWSERS_PATH: process.env.PLAYWRIGHT_BROWSERS_PATH || defaultPath}; const cli=path.join(process.cwd(),'node_modules','@playwright','test','cli.js'); const r=spawnSync(process.execPath,[cli,'test','--ui'],{stdio:'inherit',env}); process.exit(r.status===null?1:r.status);\"",
    "sync-version": "node scripts/sync-version.js",
    "validate-version": "node scripts/validate-version.js",
    "changelog-add-version": "node scripts/changelog-add-version-heading.js",
    "upgrade-version": "node scripts/upgrade-version.js",
    "deploy": "node scripts/deploy.js",
    "copy:vendor": "node scripts/copy-exifr.js",
    "audit:check": "npm audit --audit-level=moderate",
    "security:verify": "npm run audit:check && npm test && npm run test:e2e",
    "postinstall": "npm run copy:vendor"
  },
  "keywords": [
    "photo",
    "grid",
    "collage",
    "pwa"
  ],
  "author": "fingerfly",
  "license": "AGPL-3.0-only",
  "overrides": {
    "follow-redirects": "^1.16.0",
    "rollup": "^4.59.0",
    "undici": "^7.28.0",
    "picomatch": "^4.0.4",
    "postcss": "^8.5.10",
    "qs": "^6.15.2"
  },
  "devDependencies": {
    "@playwright/test": "^1.60.0",
    "exifr": "^7.1.3",
    "http-server": "^14.1.1",
    "jpeg-js": "^0.4.4",
    "jsdom": "^29.1.1",
    "vite": "^8.0.16",
    "vitest": "^4.1.8"
  }
}
